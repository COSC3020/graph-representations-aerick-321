function convertToAdjList(adjMatrix) {
    let matrix = [];
    let edge;
    let j;
    let list = [];
    for (let i= 0; i < adjMatrix.length -1; i++){
        matix[i] = [];
        if(edge = 1){
            list[i[j]];
        }
    }
    return adjMatrix;
}
